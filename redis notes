
r = redis.StrictRedis(host='localhost', port=6379, db=0)
r.flushdb() # flushes entire database
r.hset("blocked", "reddit.com","blah")

r.hgetall(name)
name is the hash name
Return a Python dict of the hash’s name/value pairs